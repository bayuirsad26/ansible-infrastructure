---
# Optimized Logging Role - Simple & Effective
# Perfect for centralized platforms like Loki, Elasticsearch

# Basic logging configuration
logging_enable_forwarding: false
logging_destination: ""  # e.g., "loki-server:514" or "10.0.0.100:514"
logging_protocol: "tcp"

# System logging components
logging_enable_rsyslog: true
logging_enable_journald: true

# Log format - keep simple for basic setup
logging_format: "traditional"

# Container logging integration
logging_container_aware: true
logging_docker_logs: true

# Log retention and rotation
logging_retention_days: 30
logging_max_log_size: "100M"
logging_rotation_frequency: "daily"

# Log cleanup
logging_enable_cleanup: true
