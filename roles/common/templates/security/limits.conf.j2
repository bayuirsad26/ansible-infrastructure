# /etc/security/limits.conf
# Managed by Ansible - Do not edit manually
#
# Each line describes a limit for a user in the form:
#
# <domain>        <type>  <item>  <value>
#

# End of file
{% for limit in common_security_limits %}
{{ limit.domain }}    {{ limit.type }}    {{ limit.item }}    {{ limit.value }}
{% endfor %}

# Additional security limits
*               soft    core            0
*               hard    core            0
root            soft    core            unlimited
root            hard    core            unlimited

# Process limits
*               soft    maxlogins       10
*               hard    maxlogins       10

# Memory limits for non-root users
*               soft    as              unlimited
*               hard    as              unlimited

# Default limits for all users
*               soft    fsize           unlimited
*               hard    fsize           unlimited
*               soft    cpu             unlimited
*               hard    cpu             unlimited
*               soft    data            unlimited
*               hard    data            unlimited
*               soft    stack           8192
*               hard    stack           8192
*               soft    rss             unlimited
*               hard    rss             unlimited
*               soft    memlock         64
*               hard    memlock         64
*               soft    priority        0
*               hard    priority        0
*               soft    locks           unlimited
*               hard    locks           unlimited
*               soft    sigpending      62793
*               hard    sigpending      62793
*               soft    msgqueue        819200
*               hard    msgqueue        819200
